# 2024-discord-bot-js

A Discord music bot written in Node.js using discord.js v14.

## Requirements

- Node.js 18 or newer
- A Discord bot token

## Setup

1. Install dependencies: `npm install`
2. Create a `.env` file with your bot token.
3. Run: `node index.js`
